import React from "react";
///import { BrowserRouter as Router } from "react-router-dom";

const ServiceDetails = (props) => {
  return (
    <div>
      <h1>{props.param.id}</h1>
    </div>
  );
};

export default ServiceDetails;
